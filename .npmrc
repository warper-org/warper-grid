# Private registry configuration for @itsmeadarsh scope
# Uncomment and configure the registry URL for private distribution
# @itsmeadarsh:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# Alternative: Use a private npm registry
# @itsmeadarsh:registry=https://your-private-registry.com
# //your-private-registry.com/:_authToken=${NPM_TOKEN}

# For Verdaccio or similar local registry:
# @itsmeadarsh:registry=http://localhost:4873

# Access configuration
access=restricted
